[build]
  command = "pnpm --filter xclusive-cuts run build:netlify"
  publish = "artifacts/xclusive-cuts/dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
